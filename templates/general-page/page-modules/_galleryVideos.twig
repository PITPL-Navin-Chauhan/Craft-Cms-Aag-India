<section>
    <div class="container-xl">
        <div class="row">
            <div class="col-sm-12">
                <div class="gallery_img">
                    {% set videoUrl = pageModule.videoLink.all() %}
                    <div class="row">
                        {% for videoLink in videoUrl %}
                            <div class=" col-md-4 col-lg-4 col-sm-12 pb-4">
                                <iframe width="100%" height="315" src="{{ videoLink.videoLinkUrl }}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"allowfullscreen>
                                </iframe>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>